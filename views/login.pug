extends layout 
block content
    h1.mb-5 #{title}
    form(method="POST", action="/login")
        .mb-3
          label.form-label(for='username') Ism
          input#username.form-control(type='text', name="username")
        .mb-3
          label.form-label(for='password') Parolni kiriting
          input#password.form-control(type='password', name="password")          
        button.btn.btn-info(type='submit') Kirish
